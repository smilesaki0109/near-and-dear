import { HomeBrowse } from "@/components/home/HomeBrowse";

/** Single home route: browse (search, chips, grid). Create/share comes later. */
export default function HomePage() {
  return <HomeBrowse />;
}
